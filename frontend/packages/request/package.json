{
  "name": "@project_neko/request",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "统一请求库，支持 Web / React Native，提供 Axios、Token 刷新、请求队列等功能",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "main": "index.ts",
  "browser": "index.web.ts",
  "types": "index.ts",
  "react-native": "index.native.ts",
  "exports": {
    ".": {
      "react-native": "./index.native.ts",
      "browser": "./index.web.ts",
      "default": "./index.ts"
    },
    "./web": "./index.web.ts"
  },
  "scripts": {
    "build": "vite build --config vite.config.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:native": "tsc --noEmit -p tsconfig.native.json",
    "test": "vitest run",
    "prepublishOnly": "npm run build && node -e \"console.error('\\n[@project_neko/request] 这是 N.E.K.O monorepo 内部 workspace 包（package.json 入口刻意指向 TS 源码，并非可发布的 dist 产物）。\\n如需发布到 npm，请先实现 dist 编译产物并更新 main/types/exports 指向 dist，再移除 private/prepublishOnly 防护。\\n'); process.exit(1)\""
  },
  "keywords": [
    "axios",
    "request",
    "token-refresh",
    "request-queue",
    "web",
    "react-native"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.13.2",
    "axios-auth-refresh": "^3.3.6"
  },
  "peerDependencies": {
    "@react-native-async-storage/async-storage": "^1.0.0"
  },
  "peerDependenciesMeta": {
    "@react-native-async-storage/async-storage": {
      "optional": true
    }
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.1.3"
  }
}
