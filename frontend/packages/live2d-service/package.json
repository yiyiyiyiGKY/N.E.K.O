{
  "name": "@project_neko/live2d-service",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "跨平台 Live2D Service：统一 Web(PixiJS+Cubism) / React Native(原生Cubism) / 旧 HTML/JS 的控制接口",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "main": "index.ts",
  "types": "index.ts",
  "react-native": "index.native.ts",
  "exports": {
    ".": {
      "react-native": "./index.native.ts",
      "default": "./index.ts"
    },
    "./web": "./index.web.ts"
  },
  "scripts": {
    "build": "vite build --config vite.config.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:native": "tsc --noEmit -p tsconfig.native.json",
    "prepublishOnly": "npm run build && node -e \"console.error('\\n[@project_neko/live2d-service] 这是 N.E.K.O monorepo 内部 workspace 包（package.json 入口刻意指向 TS 源码，并非可发布的 dist 产物）。\\n如需发布到 npm，请先实现 dist 编译产物并更新 main/types/exports 指向 dist，再移除 private/prepublishOnly 防护。\\n'); process.exit(1)\""
  },
  "keywords": [
    "live2d",
    "cubism",
    "pixijs",
    "react-native",
    "service"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "vite": "^7.1.11"
  }
}

