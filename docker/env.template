# N.E.K.O. Docker 环境变量配置模板
# 复制此文件为 .env 并填入你的配置

# ==================== 必填项 ====================

# 核心 API Key（必填）
# 从你选择的 API 提供商获取（阿里云/OpenAI/智谱/阶跃星辰）
NEKO_CORE_API_KEY=your-api-key-here

# ==================== API 提供商选择 ====================

# 核心 API 提供商
# 可选值: qwen（阿里云）, openai, glm（智谱）, step（阶跃星辰）, free（免费版）
NEKO_CORE_API=qwen

# 辅助 API 提供商（用于记忆管理、情感分析等）
# 可选值: qwen, openai, glm, step, silicon（硅基流动）, grok, doubao（豆包）
NEKO_ASSIST_API=qwen

# ==================== 其他 API Keys（可选）====================

# 如果你想使用不同的 API 提供商，在这里设置对应的 API Key
# 留空则使用 NEKO_CORE_API_KEY

# 阿里云（Qwen）API Key
NEKO_ASSIST_API_KEY_QWEN=

# OpenAI API Key
NEKO_ASSIST_API_KEY_OPENAI=

# 智谱（GLM）API Key
NEKO_ASSIST_API_KEY_GLM=

# 阶跃星辰（Step）API Key
NEKO_ASSIST_API_KEY_STEP=

# Grok（xAI）API Key
NEKO_ASSIST_API_KEY_GROK=

# 豆包（字节跳动）API Key
NEKO_ASSIST_API_KEY_DOUBAO=

# 硅基流动（Silicon）API Key
NEKO_ASSIST_API_KEY_SILICON=

# MCP Router Token（如果使用 MCP 功能）
NEKO_MCP_TOKEN=

# ==================== 服务器端口（可选）====================

# 主服务器端口（Web UI）
NEKO_MAIN_SERVER_PORT=48911

# 记忆服务器端口
NEKO_MEMORY_SERVER_PORT=48912

# 监控服务器端口
NEKO_MONITOR_SERVER_PORT=48913

# Agent/工具服务器端口
NEKO_TOOL_SERVER_PORT=48915

# ==================== 模型配置（高级，可选）====================

# 这些设置会覆盖 API 提供商的默认模型
# 通常不需要修改，除非你知道自己在做什么

# 摘要生成模型
# NEKO_SUMMARY_MODEL=qwen-plus

# 文本纠错模型
# NEKO_CORRECTION_MODEL=qwen-max

# 情感分析模型
# NEKO_EMOTION_MODEL=qwen-turbo

# 视觉理解模型
# NEKO_VISION_MODEL=qwen3-vl-plus-2025-09-23

# ==================== MCP Router（高级，可选）====================

# MCP Router URL
# NEKO_MCP_ROUTER_URL=http://localhost:3283

# ==================== 说明 ====================

# 1. 环境变量优先级高于配置文件
# 2. 如果你想使用配置文件而不是环境变量，可以挂载 config/ 目录
# 3. 至少需要设置 NEKO_CORE_API_KEY
# 4. 推荐使用阿里云（qwen），有免费额度且功能全面

# ==================== 快速配置示例 ====================

# 阿里云（推荐，有免费额度）:
#   NEKO_CORE_API_KEY=sk-xxxxx
#   NEKO_CORE_API=qwen
#   NEKO_ASSIST_API=qwen

# OpenAI:
#   NEKO_CORE_API_KEY=sk-xxxxx
#   NEKO_CORE_API=openai
#   NEKO_ASSIST_API=openai

# 智谱（完全免费）:
#   NEKO_CORE_API_KEY=xxxxx
#   NEKO_CORE_API=glm
#   NEKO_ASSIST_API=glm

# 免费版（无需 API Key，功能有限）:
#   NEKO_CORE_API_KEY=free-access
#   NEKO_CORE_API=free
#   NEKO_ASSIST_API=free

# Grok（xAI，国内无法使用）:
#   NEKO_CORE_API_KEY=xai-xxxxx
#   NEKO_ASSIST_API=grok
#   NEKO_ASSIST_API_KEY_GROK=xai-xxxxx

# 豆包（字节跳动）:
#   NEKO_ASSIST_API=doubao
#   NEKO_ASSIST_API_KEY_DOUBAO=your-doubao-api-key
